using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class TestForEvents : MonoBehaviour
{
    void Awake()
    {
        Debug.Log($"Awake");
    }

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log($"Start");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
